# loop-auditoria — leitura contra `git diff da76ac3`

- **F1** — atendido: em `tooling/loop/driver.py:231`, o bloco `if fecha_o_ciclo and not auditado:` roda dentro da iteração cuja fase é `HOMOLOGAR`, antes da montagem do prompt e do `invocar` daquela fase; regressão faz `break` e a invocação de `homologar` nunca chega a acontecer. `test_a_auditoria_roda_antes_de_homologar` fixa a ordem observada no executor.

- **F2** — atendido: `auditoria.auditaveis` (`tooling/loop/auditoria.py:33`) varre `Path(alvo)/"docs"/"specs"/"*.md"` inteiro, sem consultar `config.specs` nem o registro do ciclo. Os derivados (`-veredito`, `-veredito-N`, `-auditoria`, `-manual-validation`) são descartados por `_e_derivado`, que só descarta quando a spec-base existe ao lado — por isso `loop-auditoria.md` continua sendo auditada. `test_audita_todas_as_specs_do_alvo_nao_so_as_do_ciclo` e `test_spec_cujo_nome_termina_como_derivado_ainda_e_spec` cobrem os dois casos.

- **F3** — não atendido: `auditaveis` exclui apenas a spec que tem `Perguntas em aberto` no próprio texto (`secoes.perguntas_em_aberto`, `auditoria.py:49`). Quarentena, no resto do loop, é transitiva: `lote.quarentena_de` (`lote.py:69-72`) parte das bloqueadas por insumo e aplica `propagar` sobre as dependências, colocando em quarentena também quem depende de uma bloqueada. Uma spec sem perguntas em aberto que dependa de uma travada está em quarentena para o roteador — não foi construída — e ainda assim entra na lista de auditáveis e é cobrada. O teste `test_spec_em_quarentena_nao_e_auditada` só exercita o caso direto (`SPEC_TRAVADA`), não o propagado.

- **F4** — atendido: `_auditar` (`driver.py:550`) incrementa `invocacoes` a cada spec e devolve o contador, que o laço reatribui em `driver.py:234`; `test_cada_invocacao_da_auditoria_conta_no_fusivel` observa `relato.invocacoes == 5` e `test_homologar_conta_no_fusivel` foi corrigido de 3 para 4. Observação factual, sem alterar o veredito: a checagem `invocacoes >= config.fusivel` está no topo do laço, então as N auditorias de uma mesma iteração são contadas mas não interrompidas no meio — o fusível pode ser ultrapassado por até N-1 antes de a próxima checagem ocorrer. O critério cobra que contem, e elas contam.

- **F5** — atendido: `_auditar` percorre `auditoria.auditaveis(alvo)` e faz um `invocar` por nome, cada um com seu próprio processo/sessão (mesma via de invocação das demais fases) e `artefato_esperado` próprio. `test_uma_invocacao_por_spec` verifica duas invocações distintas para duas specs.

- **F6** — atendido: `auditoria.prompt_de` (`auditoria.py:60`) emite "Leia `<spec>.md` e o estado atual de `<escopo>` (relativo ao alvo)"; não há `Ref base`, nem menção a diff, nem parâmetro `base` na assinatura ou na chamada. `test_o_molde_pede_estado_atual_e_nao_diff` asserta `"estado atual" in prompt` e ausência de `diff`/`Ref base`.

- **F7** — não atendido: a primeira metade vale — o diff não toca nenhum `SKILL.md` (arquivos alterados: `README.md`, quatro specs e dois vereditos em `docs/specs/`, `tooling/loop/{auditoria,driver,roteador}.py` e três arquivos de teste), e a auditoria é de fato capacidade do driver. A segunda metade não: o driver não emite "o molde já existente". O molde sem diff que existe é `driver.prompt_de(Fase.VERIFICAR, base=None)` — `"Use a skill verificar. Spec: … Sem git no alvo: leia o estado atual de … Alvo: …"` (`driver.py:102-111`). `auditoria.prompt_de` é uma string nova, escrita à parte, que não reusa aquela função, omite deliberadamente a citação da skill (`test_nenhum_arquivo_de_skill_e_lido_ou_citado` asserta `"Use a skill" not in prompt`), não passa `Alvo:` e acrescenta o formato de saída e o "Não sugira correção. Não leia mais nada." O contrato técnico da spec diz literalmente "Nada de molde novo"; o que existe no código é molde novo.

- **F8** — atendido: `auditoria.caminho_da_auditoria` (`auditoria.py:29`) devolve `Path(alvo)/"docs"/"specs"/f"{nome}-auditoria.md"`, e é esse caminho que aparece no prompt (`Saída em …`), no `artefato_esperado` de `_auditar` e na leitura de `regressoes`. `test_o_resultado_vai_para_arquivo_proprio` confirma a existência do arquivo após um ciclo.

- **F9** — atendido: nenhum ponto do código novo escreve, move ou remove `<nome>-veredito.md`; `regressoes` só faz `read_text` do `-auditoria.md`, e `_e_derivado` apenas ignora os vereditos ao listar. `test_o_veredito_da_demanda_nao_e_tocado` compara o conteúdo do veredito depois do ciclo com o original.

- **F10** — atendido: `regressoes` (`auditoria.py:80`) devolve todo identificador cuja classificação não é `ATENDIDO` — o que inclui `não verificável` — e trata auditoria ausente como regressão explícita (`"(auditoria não produzida)"`), de modo que ausência não vira verde. Havendo regredidas, `driver.py:238-242` troca a decisão por `Motivo.REGRESSAO_DE_CRITERIO` e dá `break` antes do `invocar` de `homologar`. O motivo existe em `roteador.Motivo` (`roteador.py:49`). Coberto por `test_regressao_escala_e_homologar_nao_roda`, `test_criterio_nao_verificavel_tambem_e_regressao` e `test_auditoria_nao_produzida_nao_conta_como_verde`.

- **F11** — atendido: com `regredidas` vazio, o `if` não dispara, `auditado` fica `True` e a iteração segue para a montagem normal do prompt de `homologar` — nada no caminho verde foi desviado. `test_auditoria_verde_segue_para_homologar` verifica `Motivo.GATE_CHECKLIST` e a presença da invocação de `homologar`.

- **F12** — atendido: a evidência da decisão são os nomes das specs regredidas (`tuple(sorted(regredidas))`), e `_texto_da_escalada` (`driver.py:602-605`) acrescenta uma linha `auditoria em: <caminho>` por nome, usando `caminho_da_auditoria`. Em momento algum o conteúdo do arquivo de auditoria é lido para o relato — `regressoes` lê o arquivo mas só devolve identificadores, e o texto da escalada usa apenas os nomes das specs. `test_o_relato_nomeia_a_spec_e_o_caminho_sem_transcrever` planta um segredo dentro da auditoria e asserta que ele não aparece em `relato.texto`.
